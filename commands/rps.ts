import Discord = require('discord.js');
export const name = "rps"
export const description = "Play rock paper scissors." // F for senko.
export function execute(message: Discord.Message, args: string[]) {
    // TODO, I committed this by accident so whoops lmao
}